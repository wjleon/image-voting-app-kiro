# =============================================================================
# AI Image Voting App - Environment Variables
# =============================================================================
# Copy this file to .env and fill in your actual values
# Never commit .env to version control!
# =============================================================================

# -----------------------------------------------------------------------------
# Database Configuration (REQUIRED)
# -----------------------------------------------------------------------------
# PostgreSQL connection string
# Format: postgresql://[user]:[password]@[host]:[port]/[database]
#
# Local Development:
DATABASE_URL="postgresql://postgres:password@localhost:5432/ai_image_voting"
#
# Vercel Postgres (Production):
# DATABASE_URL="postgres://default:xxx@xxx.postgres.vercel-storage.com:5432/verceldb?sslmode=require"
#
# Note: For serverless environments, use connection pooling (e.g., Prisma Data Proxy)

# -----------------------------------------------------------------------------
# Admin Authentication (REQUIRED)
# -----------------------------------------------------------------------------
# Password for accessing the admin dashboard at /admin
# Use a strong, unique password in production
ADMIN_PASSWORD="your-secure-password-here"

# -----------------------------------------------------------------------------
# Internationalization (OPTIONAL)
# -----------------------------------------------------------------------------
# OpenAI API key for translating prompts to Spanish
# Get your API key from: https://platform.openai.com/api-keys
# Only needed if you want to generate Spanish translations
OPENAI_API_KEY="sk-..."

# OpenAI model to use for translations (default: gpt-3.5-turbo)
# Options: gpt-3.5-turbo, gpt-4, gpt-4-turbo
OPENAI_MODEL="gpt-3.5-turbo"

# Supported locales (comma-separated, default: en,es)
# SUPPORTED_LOCALES="en,es"

# Default locale (default: en)
# DEFAULT_LOCALE="en"

# -----------------------------------------------------------------------------
# Application Configuration (OPTIONAL)
# -----------------------------------------------------------------------------
# Node environment (development, production, test)
NODE_ENV="development"

# Base URL for the application (used for absolute URLs in production)
# NEXT_PUBLIC_APP_URL="https://your-app.vercel.app"

# -----------------------------------------------------------------------------
# Vercel Deployment (OPTIONAL - Auto-configured by Vercel)
# -----------------------------------------------------------------------------
# These are automatically set by Vercel, no need to configure manually
# VERCEL_URL="your-app.vercel.app"
# VERCEL_ENV="production"
